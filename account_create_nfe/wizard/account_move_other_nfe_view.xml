<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_account_move_other_nfe" model="ir.ui.view">
            <field name="name">account.move.other.nfe.form</field>
            <field name="model">account.move.other.nfe</field>
            <field name="arch" type="xml">
                <form string="Reverse Journal Entry">
                    <field name="residual" invisible="1"/>
                    <field name="company_id" invisible="1"/>
                    <field name="move_ids" invisible="1"/>
                    <field name="move_type" invisible="1"/>
                    <group>
                             <!-- <field name="date_mode" string="Reversal Date" widget="radio"/> -->
                            <field name="journal_id"/>
                            <field name="date" string="Refund Date" attrs="{'invisible': ['|', ('move_type', 'not in', ('out_invoice', 'in_invoice'))]}"/>
                    </group>
                    <footer>
                        <button string='Reverse' name="reverse_moves" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
               </form>
            </field>
        </record>

        <record id="action_view_account_move_other_nfe" model="ir.actions.act_window">
            <field name="name">Reverse</field>
            <field name="res_model">account.move.other.nfe</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_account_move_other_nfe"/>
            <field name="target">new</field>
            <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
            <field name="binding_model_id" ref="account.model_account_move" />
            <field name="binding_view_types">list</field>
        </record>
    </data>
</odoo>
