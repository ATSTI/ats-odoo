<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Customer Invoice views -->
    <record id="invoice_fatura_duplicata_form" model="ir.ui.view">
        <field name="name">l10n_br_account.invoice.fatura.duplicata.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="priority">299</field>
        <field name="arch" type="xml">
            <label for="journal_id" position="before">
                <field name="fatura_duplicata"/>
            </label>
        </field>
    </record>

</odoo>
