<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
            <record id="purchase_order_purchase_itens_form_inherit" model="ir.ui.view">
            <field name="name">purchase.order.itens.form.inherit</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="/form/sheet/div/button[@name='action_view_picking']" position="before">
                    <button type="object" name="abrir_item_pedidos" class="oe_stat_button" icon="fa-money" attrs="{'invisible':[('state', 'in', ('draft','sent','to approve'),('purchase_itens_ref','=',False))]}">
                        <field name="update_price_count" widget="statinfo"  string="Definir Preços"/>
                        <field name="purchase_itens_ref" invisible="1"/>

                    </button>
                </xpath>
            </field>
        </record>
    </data>
</odoo>