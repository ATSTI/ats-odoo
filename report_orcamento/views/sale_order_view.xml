<odoo>
    <record id="locefaca_view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_order_tag.sale_order_tag_view_form" />
        <field name="arch" type="xml">
            <field name="so_tag_ids" position="replace"/>
            <field name="commitment_date" position="replace"/>
            <field name="tag_ids" position="replace"/>
            <field name="partner_id" position="after">
                <field name="so_tag_ids" 
                    string="Entregáveis"
                    placeholder="Materiais..."
                    widget="many2many_tags"
                    options="{'color_field': 'color', 'no_create': True, 'no_create_edit': True}"
                    domain="['|',('team_ids', 'in', team_id), ('team_ids', '=', False)]" 
                />
                <field name="commitment_date" />
                <field name="tag_ids" 
                    string="Equipe" 
                    placeholder="colaboradores..."
                    widget="many2many_tags"
                />
            </field>
        </field>
    </record>
</odoo>
